# Add the API key for your chosen provider.
# See https://docs.litellm.ai/docs/providers for the full list.

# OPENAI_API_KEY="sk-..."
# OPENROUTER_API_KEY="sk-or-..."

# ANTHROPIC_API_KEY="sk-ant-..."
# but keep in mind anthropic does not like you using your subscription key in third party apps, 
# so you might want to create a separate key with limited permissions and not use a subscription but pay api rates 
# they are very unclear on whats allowed and whats not, so proceed with caution and consider using a different provider if you want to avoid potential issues with your key