[project]
name = "simpleharness"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "litellm>=1.67.0",
    "python-dotenv>=1.1.0",
    "pyyaml>=6.0.3",
    "rich>=13.0.0",
]

[project.scripts]
simpleharness = "main:main"
